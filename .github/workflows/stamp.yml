name: Stamp Versioning

on:
  pull_request:
    branches:
      - main
    types:
      - closed

jobs:
  tag_version:
    if: ${{ github.event.pull_request.merged }} && ${{ github.event.pull_request.base.ref == 'main' }}

    runs-on: ubuntu-latest

    steps:
      - name: Parse Branch Name
        id: parse_branch
        run: |
          branch_name=$(echo "${{ github.event.pull_request.base.ref }}" | awk -F'/' '{print $2}')
          if [[ $branch_name =~ ^(major|minor|patch)$ ]]; then
            echo "VERSION_TYPE=$branch_name" >> $GITHUB_ENV
          fi

      - name: Print Environment Variable
        if: env.VERSION_TYPE
        run: echo $VERSION_TYPE
        
      # - name: Generate a token
      #   id: app-token
      #   uses: actions/create-github-app-token@v1
      #   with:
      #     app-id: ${{ vars.APP_ID }}
      #     private-key: ${{ secrets.APP_KEY }}

      # - name: Checkout code
      #   uses: actions/checkout@v4
      #   with:
      #     token: ${{ steps.app-token.outputs.token }}

      # - name: Create tag
      #   run: |
      #     chmod +x ./.github/scripts/create_tag.sh
      #     echo "VERSION_TAG=$(echo $(./.github/scripts/create_tag.sh ${{ env.BRANCH_PREFIX }}))" >> $GITHUB_ENV

      # - name: Push tag
      #   if: env.VERSION_TAG
      #   run: |
      #     echo "VERSION_TAG=${{ env.VERSION_TAG }}"
      #     git config user.name "GitHub Actions Bot"
      #     git config user.email "<>"
      #     git tag ${{ env.VERSION_TAG }} -m "Version ${{ env.VERSION_TAG }}"
      #     git push origin ${{ env.VERSION_TAG }}
