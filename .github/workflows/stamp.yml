name: Stamp Versioning

on:
  workflow_dispatch:
  pull_request:
    types:
      - closed

jobs:
  tag_version:
    if: ${{ github.event.pull_request.merged }} && ${{ github.base_ref == 'main' }}

    runs-on: ubuntu-latest

    steps:
      - name: Generate a token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_KEY }}

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ steps.app-token.outputs.token }}
          ref: ${{ github.head_ref }}
          persist-credentials: false

      - name: Setup git config
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "<>"

      - name: Create tag
        id: create_tag
        run: |
            chmod +x .stamp/scripts/stamp.sh
            VERSION_TAG=$(echo ./.stamp/scripts/stamp.sh ${{ github.event.pull_request.head.ref }}) >> $GITHUB_OUTPUT
            echo "VERSION_TAG=$VERSION_TAG"
          # chmod +x .stamp/scripts/stamp.sh
          # .stamp/scripts/stamp.sh ${{ github.event.pull_request.head.ref }}
